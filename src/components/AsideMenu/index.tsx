import React, { useState } from "react";
import "./style.scss";

const AsdieMunu: React.FC<{ history?: any }> = ({ history }) => {
  return (
    <React.Fragment>
      <div className="aside-containter">Aside menu</div>
    </React.Fragment>
  );
};

export default AsdieMunu;
